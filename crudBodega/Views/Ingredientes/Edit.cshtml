@model crudBodega.Models.Ingrediente

@{
    ViewData["Title"] = "Edit";
}

<h1>Editar</h1>

<h4>Ingrediente</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Idingrediente" />
            <div class="form-group">
                <label asp-for="Nombreingrediente" class="control-label">Nombre Ingrediente</label>
                <input asp-for="Nombreingrediente" class="form-control" />
                <span asp-validation-for="Nombreingrediente" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Precioingrediente" class="control-label">Precio Ingrediente</label>
                <input asp-for="Precioingrediente" class="form-control" />
                <span asp-validation-for="Precioingrediente" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Volver a la lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
