@model IEnumerable<financeApp.Models.Egreso>

@{
    ViewData["Title"] = "Principal";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Montoegreso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fechaegreso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcionegreso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Montoegreso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Fechaegreso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcionegreso)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idegreso">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idegreso">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idegreso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
